import { appSelectors } from './appSelectors'
import { authSelectors } from './authSelectors'
import { tasksSelectors } from './tasksSelectors'
import { todoListsSelectors } from './todoListsSelectors'

export const selectors = {
  appSelectors,
  authSelectors,
  tasksSelectors,
  todoListsSelectors,
}
